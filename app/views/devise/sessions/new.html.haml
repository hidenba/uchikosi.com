%h2 ログイン

= form_for resource, :as => resource_name, :url => session_path(resource_name) do |f|
  %p= f.label :login_name
  %p= f.text_field :login_name

  %p= f.label :password
  %p= f.password_field :password

  - if devise_mapping.rememberable?
    %p
      = f.check_box :remember_me
      = f.label :remember_me

  %p= f.submit "ログイン"